apiVersion: apps/v1
kind: Deployment
metadata:
  name: valentine
spec:
  replicas: 1
  selector:
    matchLabels:
      app: valentine
  template:
    metadata:
      labels:
        app: valentine
    spec:
      containers:
        - name: flask-container
          image: nzhussup/valentine:latest
          ports:
            - containerPort: 80
          resources:
            requests:
              cpu: 1m
              memory: 8Mi
            limits:
              cpu: 100m
              memory: 128Mi
---
apiVersion: v1
kind: Service
metadata:
  name: valentine-service
spec:
  selector:
    app: valentine
  ports:
    - protocol: TCP
      port: 888
      targetPort: 80
  type: ClusterIP
